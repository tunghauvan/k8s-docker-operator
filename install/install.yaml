# Generated by make dist on Thu Feb 12 23:54:35 +07 2026
# Version: v0.0.26
---
apiVersion: v1
kind: Namespace
metadata:
  name: system
---
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: dockercontainers.app.example.com
spec:
  group: app.example.com
  names:
    kind: DockerContainer
    listKind: DockerContainerList
    plural: dockercontainers
    singular: dockercontainer
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: DockerContainer is the Schema for the dockercontainers API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DockerContainerSpec defines the desired state of DockerContainer
            properties:
              command:
                description: Command to run in the container
                items:
                  type: string
                type: array
              containerName:
                description: ContainerName is the name of the container on the docker
                  host
                type: string
              dockerHostRef:
                description: |-
                  DockerHostRef is the name of the DockerHost CR to use.
                  If empty, defaults to the local Docker socket.
                type: string
              env:
                description: Env list of environment variables (e.g. "KEY=VALUE")
                items:
                  type: string
                type: array
              envVars:
                description: EnvVars list of environment variables with support for
                  ValueFrom
                items:
                  description: EnvVar defines an environment variable
                  properties:
                    name:
                      description: Name of the environment variable
                      type: string
                    value:
                      description: Value of the environment variable (literal)
                      type: string
                    valueFrom:
                      description: ValueFrom source for the environment variable's
                        value
                      properties:
                        secretKeyRef:
                          description: SecretKeyRef selects a key of a secret in the
                            target container's namespace
                          properties:
                            key:
                              description: The key of the secret to select from. Must
                                be a valid secret key.
                              type: string
                            name:
                              description: Name of the referent
                              type: string
                          required:
                          - key
                          - name
                          type: object
                      type: object
                  required:
                  - name
                  type: object
                type: array
              healthCheck:
                description: HealthCheck defines a Docker health check for the container
                properties:
                  interval:
                    description: Interval between checks (e.g. "30s")
                    type: string
                  retries:
                    description: Retries before reporting unhealthy
                    type: integer
                  test:
                    description: Test is the command to run (e.g. ["CMD", "curl",
                      "-f", "http://localhost/"])
                    items:
                      type: string
                    type: array
                  timeout:
                    description: Timeout for a single check (e.g. "5s")
                    type: string
                required:
                - test
                type: object
              image:
                description: Image is the Docker image to run
                type: string
              imagePullSecret:
                description: ImagePullSecret is the name of the Secret containing
                  Docker registry credentials
                type: string
              networkMode:
                description: NetworkMode for the container (e.g. "host", "bridge",
                  "kind")
                type: string
              ports:
                description: Ports list of port mappings (e.g. "8080:80")
                items:
                  type: string
                type: array
              resources:
                description: Resources defines CPU and Memory limits for the container
                properties:
                  cpuLimit:
                    description: CPULimit in cores (e.g. "0.5" = half a core, "2"
                      = 2 cores)
                    type: string
                  memoryLimit:
                    description: MemoryLimit in bytes with optional suffix (e.g. "256m",
                      "1g")
                    type: string
                type: object
              restartPolicy:
                description: RestartPolicy for the container (no, on-failure, always,
                  unless-stopped)
                type: string
              secretVolumes:
                description: SecretVolumes list of Kubernetes Secrets to mount as
                  files
                items:
                  description: SecretVolume defines a mapping from a K8s Secret to
                    a directory in the container
                  properties:
                    mountPath:
                      description: MountPath is the absolute path in the container
                        where the secret should be mounted
                      type: string
                    secretName:
                      description: SecretName is the name of the Kubernetes Secret
                      type: string
                  required:
                  - mountPath
                  - secretName
                  type: object
                type: array
              services:
                description: Services defines the ports to expose via Kubernetes Service
                items:
                  properties:
                    name:
                      description: Name is the name of the port
                      type: string
                    port:
                      description: Port is the port number to expose on the Kubernetes
                        Service
                      format: int32
                      type: integer
                    targetPort:
                      description: TargetPort is the port number on the container
                        to forward to
                      format: int32
                      type: integer
                  required:
                  - port
                  - targetPort
                  type: object
                type: array
              volumeMounts:
                description: VolumeMounts list of volumes to mount into the container
                items:
                  description: VolumeMount defines a volume mount
                  properties:
                    containerPath:
                      description: ContainerPath is the path in the container
                      type: string
                    hostPath:
                      description: HostPath is the path on the host
                      type: string
                    readOnly:
                      description: ReadOnly if true, mounts the volume as read-only
                      type: boolean
                  required:
                  - containerPath
                  - hostPath
                  type: object
                type: array
            type: object
          status:
            description: DockerContainerStatus defines the observed state of DockerContainer
            properties:
              health:
                description: Health is the Docker health status (healthy, unhealthy,
                  starting, none)
                type: string
              id:
                description: ID of the container
                type: string
              state:
                description: State request state of the container
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: dockerhosts.app.example.com
spec:
  group: app.example.com
  names:
    kind: DockerHost
    listKind: DockerHostList
    plural: dockerhosts
    singular: dockerhost
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: DockerHost is the Schema for the dockerhosts API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DockerHostSpec defines the desired state of DockerHost
            properties:
              hostURL:
                description: HostURL is the URL of the Docker Daemon (e.g. tcp://1.2.3.4:2376
                  or unix:///var/run/docker.sock)
                type: string
              tlsSecretName:
                description: TLSSecretName is the name of the Secret containing ca.pem,
                  cert.pem, key.pem
                type: string
            type: object
          status:
            description: DockerHostStatus defines the observed state of DockerHost
            properties:
              message:
                description: Message info about the connection status
                type: string
              phase:
                description: Phase indicates if we can connect to the host (Connected,
                  Error)
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: manager-role
rules:
- apiGroups:
  - ""
  resources:
  - nodes
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - ""
  resources:
  - secrets
  verbs:
  - create
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - ""
  resources:
  - services
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - app.example.com
  resources:
  - dockercontainers
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - app.example.com
  resources:
  - dockercontainers/finalizers
  verbs:
  - update
- apiGroups:
  - app.example.com
  resources:
  - dockercontainers/status
  verbs:
  - get
  - patch
  - update
- apiGroups:
  - app.example.com
  resources:
  - dockerhosts
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - apps
  resources:
  - deployments
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - coordination.k8s.io
  resources:
  - leases
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: tunnel-gateway
  namespace: system
  labels:
    app: tunnel-gateway
spec:
  replicas: 1
  selector:
    matchLabels:
      app: tunnel-gateway
  template:
    metadata:
      labels:
        app: tunnel-gateway
    spec:
      containers:
      - name: gateway
        image: hvtung/k8s-docker-operator:v0.0.26 # Will be updated by release
        command: ["/manager", "tunnel", "-mode", "gateway", "-gateway-addr", ":8080"]
        ports:
        - containerPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: tunnel-gateway
  namespace: system
spec:
  selector:
    app: tunnel-gateway
  type: NodePort
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30000 # Fixed NodePort as requested
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: controller-manager
  namespace: system
  labels:
    control-plane: controller-manager
spec:
  selector:
    matchLabels:
      control-plane: controller-manager
  replicas: 1
  template:
    metadata:
      labels:
        control-plane: controller-manager
    spec:
      serviceAccountName: controller-manager
      containers:
      - command:
        - /manager
        args:
        - --leader-elect
        image: hvtung/k8s-docker-operator:v0.0.26
        name: manager
        securityContext:
          runAsUser: 0
          runAsNonRoot: false
        env:
        - name: OPERATOR_IMAGE
          value: hvtung/k8s-docker-operator:v0.0.26
        volumeMounts:
        - mountPath: /var/run/docker.sock
          name: docker-sock
        resources:
          limits:
            cpu: 500m
            memory: 128Mi
          requests:
            cpu: 10m
            memory: 64Mi
      volumes:
      - name: docker-sock
        hostPath:
          path: /var/run/docker.sock
          type: Socket
      terminationGracePeriodSeconds: 10
